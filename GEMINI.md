# Antigravity 開発ガイドライン & プロジェクトルール

このリポジトリ（team-monthly-slackbot）で作業するすべてのエージェントおよび開発者が遵守すべき共通規律です。

---

## 1. コミット・プッシュ前の総合検証（必須）
- コード変更やリファクタリングを行った後は、プッシュする前に必ず以下を実行すること：
  ```bash
  npm run verify
  ```
- 内部で ESLint、TypeScriptビルド、単体テスト、ハードコード監査、Slack構文チェック、本番ヘルスチェックの6層検査が実行されます。すべて PASS することを確認してからコミット・プッシュすること。

---

## 2. Slack mrkdwn & 日本語テキスト整形ルール
- **太字の境界ルール**:
  - `・*太字*` はSlackで太字として解釈されない。箇条書きリストで太字を使う際は必ず `・ *太字*` と半角スペースを空けること。
  - `*太字*：` のように全角コロンが直結すると太字が無効化される。必ず `*太字*: `（半角コロン＋半角スペース）に置換すること。
  - AI（Gemini等）が生成したテキストをSlackに送信する際は、上記を自動サニタイズするパイプライン（`formatMarkdownForSlack`）を必ず通すこと。
- **通知の二重投稿防止**:
  - 週報AI要約などの機微なレポートは、チャンネルに長文エフェメラルを出さずDMに集約する。チャンネル側は1行の完了通知のみ返すこと。

---

## 3. メンバー管理 & メール判定ルール
- コード内に個人名、社員番号（6桁数値）、個人メールアドレスをハードコードしない。必ず `TEAM_MEMBERS_CONFIG`（JSON配列）から動的に取得すること。
- メールの抽出・振り分けは、除外ブラックリストではなくホワイトリストアドレス（`ANNOUNCEMENT_TO_EMAILS`, `MANAGER_REPORT_FROM_EMAILS`）で判定すること。

---

## 4. GCP Cloud Run 運用ルール
- 費用完全0円運用のため、`--min-instances 0` を維持すること。
- ローカル開発（Socket Mode）と本番（Webhook）は `SLACK_SOCKET_MODE` 環境変数で切り替えること。
- デプロイは GitHub Actions と Workload Identity Federation (WIF) による自動OIDC認証を使用し、秘密鍵JSONを作成・配置しないこと。
